const fs = require("fs"); let c = fs.readFileSync("src/app/(dashboard)/ai-asistan/randevu/RandevuClient.tsx", "utf8"); const target = `if (selectedDate !== today) {\n      loadDate();\n    } else {\n      setAppointments(initialAppointments);\n    }`; const replacement = `if (selectedDate !== today || activeCalendarId) {\n      loadDate();\n    } else {\n      setAppointments(initialAppointments);\n    }`; if(c.includes(target)) { fs.writeFileSync("src/app/(dashboard)/ai-asistan/randevu/RandevuClient.tsx", c.replace(target, replacement), "utf8"); console.log("OK"); } else { console.log("TARGET NOT FOUND"); }
