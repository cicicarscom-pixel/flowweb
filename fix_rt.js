const fs = require("fs"); let c = fs.readFileSync("src/app/(dashboard)/ai-asistan/randevu/RandevuClient.tsx", "utf8"); c = c.replace(/const \{ data \} = await getAppointmentsByDate\(selectedDate\);/g, "const { data } = await getAppointmentsByDate(selectedDate, activeCalendarId || undefined);"); fs.writeFileSync("src/app/(dashboard)/ai-asistan/randevu/RandevuClient.tsx", c, "utf8"); console.log("OK");
