const fs = require("fs"); let c = fs.readFileSync("src/app/(dashboard)/ai-asistan/randevu/RandevuClient.tsx", "utf8"); const target = `<span style={{ fontSize: 12, color: "var(--text-secondary)", background: "rgba(255,255,255,0.05)", padding: "2px 8px", borderRadius: 99 }}>{svcName}</span>`; const replacement = `<div style={{ display: "flex", gap: 8 }}>\n                            <span style={{ fontSize: 12, color: "var(--text-secondary)", background: "rgba(255,255,255,0.05)", padding: "2px 8px", borderRadius: 99 }}>{svcName}</span>\n                            {multiCalendarEnabled && activeCalendarId === null && appt.calendar_id && (\n                              <span style={{ fontSize: 12, color: "#22B573", background: "rgba(34,181,115,0.1)", padding: "2px 8px", borderRadius: 99 }}>\n                                {calendars.find((c: any) => c.id === appt.calendar_id)?.name || "Takvim"}\n                              </span>\n                            )}\n                          </div>`; if(c.includes(target)) { fs.writeFileSync("src/app/(dashboard)/ai-asistan/randevu/RandevuClient.tsx", c.replace(target, replacement), "utf8"); console.log("OK"); } else { console.log("TARGET NOT FOUND"); }
